# FitTrack V2

一个可直接部署到 Vercel 的 React/Vite 健身记录 App。

## 使用

```bash
npm install
npm run build
npm run dev
```

## 部署

把整个项目上传到 GitHub，然后在 Vercel 导入仓库即可。构建命令为 `npm run build`，输出目录为 `dist`。

## 数据

训练、饮食、体重、收藏动作等数据保存在手机浏览器本地存储中。更换手机或清除浏览器数据会丢失记录。
