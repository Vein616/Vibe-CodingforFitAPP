# 部署到手机详细步骤

1. 注册或登录 Vercel。
2. 把本项目上传到 GitHub。
3. 在 Vercel 选择 `Add New Project`，导入这个 GitHub 仓库。
4. 保持默认配置：构建命令 `npm run build`，输出目录 `dist`。
5. 部署完成后，用 iPhone Safari 打开 Vercel 网址。
6. 点击 Safari 分享按钮，选择“添加到主屏幕”。

FitTrack V2 使用浏览器本地存储，不需要额外数据库。
